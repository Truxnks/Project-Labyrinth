# SerpentPath
4-tier corporate network simulation built on physical hardware — progressively hardened across 5 versions. Full attack chain: Windows DPAPI → SSH pivoting → command injection → SUID escape → root.
